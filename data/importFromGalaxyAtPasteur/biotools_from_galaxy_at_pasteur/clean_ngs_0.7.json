{
    "collectionName": "GalaxyAtPasteur", 
    "sourceRegistry": "toolshed-pasteur", 
    "description": "Use adapters definition file ADAPTER.txt and remove the adapters from the input read files. The program writes out the cleansed reads and the rejected reads. Nucleotides are also removed based on quality thresholds from the 5' and 3'.", 
    "platform": "Linux", 
    "maturity": "production", 
    "version": "0.7", 
    "softwareType": "Tool", 
    "id": "galaxy.web.pasteur.fr/toolshed-pasteur/repos/odoppelt/adapterremoval/clean_ngs/0.7", 
    "fonction": {
        "outputs": [
            {
                "label": "${tool.name} on ${on_string}: filtered forward sequences", 
                "name": "output_file", 
                "format": "input"
            }, 
            {
                "label": "${tool.name} on ${on_string}: filtered reverse sequences", 
                "name": "output_file2", 
                "format": "input"
            }, 
            {
                "label": "${tool.name} on ${on_string}: rejected forward sequences", 
                "name": "rejectedFQ", 
                "format": "input"
            }, 
            {
                "label": "${tool.name} on ${on_string}: rejected reverse sequences", 
                "name": "rejectedFQ2", 
                "format": "input"
            }
        ], 
        "functionHandle": "MainFunction", 
        "description": "Use adapters definition file ADAPTER.txt and remove the adapters from the input read files. The program writes out the cleansed reads and the rejected reads. Nucleotides are also removed based on quality thresholds from the 5' and 3'.", 
        "inputs": [
            {
                "label": "Adapter definition file", 
                "type": "data", 
                "name": "adapterSeqs", 
                "format": "txt"
            }
        ]
    }, 
    "name": "Clean NGS"
}